name: Keep Alive Ping

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl -sS --fail https://growth-tracker-api.yellowwater-07aa7c55.centralindia.azurecontainerapps.io/
          echo "Pinged at $(date -u)"
